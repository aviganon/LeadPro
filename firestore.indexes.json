{
  "comment": "No composite indexes needed: all queries use single-field filters (auto-indexed) or equality-only multi-field filters; ordering is done client-side. This avoids waiting for index builds after deploy.",
  "indexes": [],
  "fieldOverrides": []
}
